{
    "name": "Student react-redux-websockets-passport trivia app ",
    "version": "0.0.1",
    "description": "Quiz App implemented with React, Redux, Websockets and Passport",
    "author": "kalleja",
    "scripts": {
        "start": "cd server && npm install && node ./server.js",
        "app": "cd app && npm install && npm run app",
        "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false cd app && npm install && npm run build"
    },
    "dependencies": {
        "@material/mwc-button": "^0.14.1",
        "rmwc": "^6.0.14"
    }
}
